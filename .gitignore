node_modules/
logs/
*.log
.DS_Store

# internal business docs — never publish
PLAN.md
PUBLISH.md

# actor build artifacts
actor/node_modules/
actor/dist/
actor/storage/
REGISTRY-DRAFTS.md

# benchmark run outputs (generated; reproduce via `npm run benchmark`)
benchmark/results-*.json
